<%= form_with(url: phone_pin_url, method: "post", class: "mt-3") do %>
  <%= hidden_field_tag :employee_number, @employee_number %>
  <p class="lead text-center">Enter pin for <strong><%= @user.name %></strong>:</p>
  <div class="row">
    <div class="col-12">
      <div class="row">
        <div class="col-12">
          <%= content_tag(:div, class: "form-group") do %>
            <%= password_field_tag(:pin, "", class: ["form-control", "form-control-lg", "text-center"], readonly: true, maxlength: "4") %>
          <% end %>
        </div>
        <%= render partial: "keypad" %>
        <div class="col-12 mt-3 text-center">
          <%= link_to("<i class=\"fas fa-angle-double-left\"></i> Back".html_safe, phone_logout_url, method: :post, class: ["btn", "btn-secondary", "btn-lg"]) %>
          <%= link_to("<i class=\"fas fa-clock\"></i> Clocked In Now".html_safe, phone_now_url, class: ["btn", "btn-primary", "btn-lg"]) %>
        </div>
      </div>
    </div>
  </div>
<% end %>